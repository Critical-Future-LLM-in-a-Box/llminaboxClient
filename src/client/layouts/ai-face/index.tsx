import React from "react";
import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

const AIFace = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">AI Face</Typography>
      <Typography variant="body1">
        Placeholder for AI Face. This page will allow users to configure an AI
        avatar (static or dynamic).
      </Typography>
    </Box>
  );
};

export default AIFace;
