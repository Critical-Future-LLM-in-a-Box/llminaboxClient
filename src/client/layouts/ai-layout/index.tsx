import React from "react";
import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

const AILayout = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">AI Layout</Typography>
      <Typography variant="body1">
        Placeholder for AI Layout. This page will allow users to choose between
        bubble or full-screen layouts for the AI assistant.
      </Typography>
    </Box>
  );
};

export default AILayout;
