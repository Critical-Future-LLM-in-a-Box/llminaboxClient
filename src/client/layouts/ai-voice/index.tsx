import React from "react";
import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

const AIVoice = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">AI Voice</Typography>
      <Typography variant="body1">
        Placeholder for AI Voice. This page will allow users to configure the
        AI&apos;s voice capabilities.
      </Typography>
    </Box>
  );
};

export default AIVoice;
